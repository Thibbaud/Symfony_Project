{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <div class="container">
        <h1>Edit User</h1>

        {{ include('account/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('account_show', {'id': user.id}) }}">Retour au profil</a>

        {{ include('account/_delete_form.html.twig') }}
    </div>
{% endblock %}
